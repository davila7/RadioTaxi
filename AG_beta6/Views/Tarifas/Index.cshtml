@model IEnumerable<DataLayer6.Tarifa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Desc_Tarifa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Valor_bndra)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Valor_mts)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Desc_Tarifa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Valor_bndra)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Valor_mts)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Tarifa }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Tarifa }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Tarifa })
        </td>
    </tr>
}

</table>
